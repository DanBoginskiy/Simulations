# Брендбук навчальних симуляцій з фізики

Довідник стилю для серії інтерактивних візуалізацій. Еталонна реалізація — [Mechanics/inertial-frames.html](Mechanics/inertial-frames.html); усі значення нижче взяті з неї, а не вигадані. Другий приклад, зроблений уже за цим документом, — [basic_physics/00-kinematics-rotation.html](basic_physics/00-kinematics-rotation.html). Мета документа — щоб наступна симуляція виглядала й поводилася як частина тієї самої серії, навіть якщо її робитиме інша людина.

Структура репозиторію: брендбук лежить у корені й поширюється на всі розділи; симуляції групуються за темами шкільного курсу (`Mechanics/`, `basic_physics/`, далі — `Optics/`, `Thermodynamics/` тощо), по одному самодостатньому HTML-файлу на тему. Назву теки може задати замовник (`basic_physics/` — саме такий випадок); правило kebab-case для імені файлу лишається чинним завжди.

Поруч лежить [PROMPT-TEMPLATE.md](PROMPT-TEMPLATE.md) — заповнюваний бланк замовлення на нову симуляцію: змінюють лише тему та перелік того, що симулювати, решта вимог у ньому вже виписана.

---

## 1. Принципи

**Показувати, а не розповідати.** Текст на сторінці — лише підпис до того, що вже видно на сцені. Якщо думку можна донести рухом, кольором або взаємним розташуванням — так і роби. Абзац пояснень означає, що візуалізація не спрацювала.

**Один екран — одна ідея.** Модуль відповідає на одне питання. Якщо питань два, це два модулі.

**Число завжди поруч із картинкою.** Кожна анімована величина має живий числовий вивід. Учень має бачити, що саме змінилося, а не здогадуватися.

**Формула з підставленими числами.** Не `x′ = x − Vt`, а `x′ = −57,8 − 8,0·0,5 = 3,0 м`. Символьний запис іде першим, підставлені числа — другим, у тому ж рядку.

**Керування без пояснень.** Повзунок, перемикач, кнопка «↺» — учень має розібратися за три секунди без інструкції.

**Стриманість.** Академічний конспект, а не інфографіка. Тонкі лінії, приглушений папір, жодних тіней, градієнтів, іконок-емодзі в інтерфейсі, анімованих переходів між станами.

---

## 2. Палітра

### 2.1 Кольори систем відліку — головний носій сенсу

Три кольори закріплені за трьома системами відліку і **не міняються між модулями**. Учень запам'ятовує: синій — земля, помаранчевий — рухома СВ, зелений — друга рухома СВ.

| Роль | Токен | HEX | Що ним фарбують |
|---|---|---|---|
| СВ K — нерухома / земля / берег | `--k1` | `#2a78d6` | вісь K, чипи, серія K на графіку, підпис спостерігача |
| СВ K′ — перша рухома (поїзд, авто, вагон) | `--k2` | `#eb6834` | контур поїзда/авто/вагона, вісь K′, серія K′ |
| СВ K″ — друга рухома (велосипедист, вода, поїзд 2) | `--k3` | `#1baf7a` | контур, вісь K″, серія K″ |

Порядок слотів перевірено валідатором палітри (`dataviz/scripts/validate_palette.js`) у режимі `--pairs all` на світлій поверхні: найгірша пара за дальтонізмом ΔE 9,2 (дейтеранопія) за цілі ≥ 8; найгірша пара для звичайного зору ΔE 24,0 за порогу ≥ 15.

> **Обмеження, яке треба поважати.** Зелений `#1baf7a` має контраст 2,74:1 до паперу — нижче 3:1. Тому зелений **ніколи не несе сенсу самотужки**: біля нього завжди або чип у легенді, або прямий підпис (`K″`, `старт`), або обидва. Це не стилістична забаганка, а правило доступності.

**Більше трьох СВ на одній сцені не буває.** Якщо задача цього вимагає — розбий на два подання поруч, як зроблено в задачі про вагон.

Якщо в темі систем відліку немає взагалі, ці три слоти перепризначаються під інші ролі — але за правилами з §18, а не довільно.

### 2.2 Чорнило й поверхні

| Роль | Токен | HEX | Застосування |
|---|---|---|---|
| Папір сторінки | `--paper` | `#f9f9f7` | тло `body` |
| Картка | `--card` | `#fcfcfb` | тло `.panel`, тло полотен |
| Основне чорнило | `--ink` | `#0b0b0b` | заголовки, числа, рухоме тіло на сцені |
| Другорядне | `--sec` | `#52514e` | підписи повзунків, назви рядків, підзаголовки |
| Приглушене | `--mut` | `#898781` | підписи осей, одиниці, службові пояснення |
| Сітка | `--grid` | `#e1e0d9` | лінії сітки графіка, координатна сітка сцени |
| Вісь / орієнтир | `--axis` | `#c3c2b7` | нульова вісь, штрихові допоміжні лінії, кущі-орієнтири |
| Рамка | `--line` | `rgba(11,11,11,.10)` | межа `.panel`, кнопок, полів |

### 2.3 Кольори середовища (тільки для сцен)

| Роль | Константа | HEX | Застосування |
|---|---|---|---|
| Ґрунт, дорога, насип | `CLR.ground` | `#f3f2ee` | смуга землі під сценою |
| Вода | `CLR.water` | `#e2edfa` | заливка річки |
| Крапля / стрілка течії | `CLR.drop` | `#86b6ef` | дощ, стрілки течії |

Середовище завжди світліше за будь-який об'єкт на ньому. Воно фон, а не фігура.

### 2.4 Затемнені варіанти для дрібного тексту

Кольори СВ на світлому папері недостатньо контрастні для тексту 12–13 px. Для підписів координат у модулі 2 використано затемнені відповідники:

| Клас | HEX | Пара до |
|---|---|---|
| `.c1` | `#1c5cab` | `--k1` |
| `.c2` | `#b8431a` | `--k2` |
| `.c3` | `#0d7a55` | `--k3` |

Правило: **колір СВ — для геометрії, затемнений варіант — для тексту.**

---

## 3. Типографіка

| Роль | Шрифт | Розмір / вага |
|---|---|---|
| Заголовок сторінки | `Georgia, "Times New Roman", serif` | 21 px / 600, letter-spacing .2 px |
| Підзаголовок у заголовку | той самий serif | 14 px / 400, колір `--sec` |
| Основний текст, інтерфейс | `system-ui, -apple-system, "Segoe UI", sans-serif` | 15 px / 1.45 |
| Вкладка | sans | 14 px / 600 |
| Заголовок панелі `h3` | sans | 13 px / 600, колір `--sec` |
| Підпис модуля `.caption` | sans | 13.5 px, колір `--sec` |
| Підпис повзунка | sans | 13 px, колір `--sec` |
| Числовий вивід | sans | 13 px, `tabular-nums` |
| Службове пояснення `.formulas` | sans, курсив | 12.5 px, колір `--mut` |
| Позначка `.badge` | sans | 10.5 px / 600 |
| Текст на полотні | sans | 9,5–13 px (див. §6.6) |

**Serif — тільки для двох речей:** заголовка сторінки та фізичних змінних. Змінна в тексті загортається в `<var>` і отримує `Georgia, serif` курсивом: <var>V</var>, <var>α</var>, <var>ρ</var>. Це відрізняє змінну від звичайного слова без жодного додаткового кольору.

Ніяких великих літер урозрядку, ніякого `text-transform: uppercase`.

---

## 4. Числа й одиниці

Правила однакові в HTML і на полотні — за це відповідає єдина функція `fmt()`:

```js
const fmt=(n,d=1)=>(+n).toFixed(d).replace('.',',').replace('-','−');
```

- **Десятковий роздільник — кома.** `3,61` а не `3.61`.
- **Мінус — U+2212 (−), не дефіс.** `−57,8`. Дефіс у числі виглядає як переніс.
- **Одиниця через нерозривний контекст, завжди після числа:** `20,0 с`, `3,61 м/с`, `129°`. Градус — впритул до числа, решта — через пробіл.
- **Табличні цифри обов'язкові** там, де числа стоять стовпчиком або швидко змінюються: `font-variant-numeric: tabular-nums`. Без цього цифри «стрибають» під час анімації.
- **Розрядність фіксована** для кожної величини. Швидкість — 1 знак, координата — 1, кут — 0, безрозмірне відношення — 2. Кількість знаків не змінюється залежно від значення.

---

## 5. Розкладка

### 5.1 Каркас сторінки

```
.wrap   max-width: 1320px, padding: 18px 20px 40px, центрований
header  заголовок + підзаголовок в один рядок
.tabs   вкладки модулів, підкреслення знизу
.module один активний, решта display:none
footer  12 px, --mut, по центру
```

### 5.2 Сітки модулів

| Модуль | Сітка | Проміжок |
|---|---|---|
| Сцена + керування (модуль 1) | `330px 1fr` | 14 px |
| Два графіки поруч | `1fr 1fr` | 14 px |
| Чотири системи координат | `1fr 1fr` | 14 px |
| Задача: навігація + вміст | `176px 1fr` | 14 px |
| Задача: сцена + бічна колонка | `1fr 292px` | 14 px |
| Два подання одного руху | `1fr 1fr` | 14 px |

**Проміжок між панелями завжди 14 px.** Внутрішні відступи панелі — `14px 16px`, для панелей у бічній колонці — `12px 14px`.

### 5.3 Адаптив

Одна контрольна точка: `max-width: 900px`. Усі багатоколонкові сітки стають однією колонкою, бічна навігація задач із вертикальної стає горизонтальною і втрачає `position: sticky`.

Горизонтальної прокрутки не буває ніколи. Полотна мають `width: 100%` і фіксовану висоту в px; при зміні розміру вікна кеш полотен скидається і все перемальовується.

---

## 6. Компоненти

### 6.1 Панель `.panel`

Тло `--card`, рамка 1 px `--line`, радіус 10 px. Без тіні. Заголовок `h3` — 13 px/600 кольору `--sec`, відступ знизу 10 px.

### 6.2 Вкладки `.tabs`

Кнопка без тла, підкреслення 2 px прозоре; активна — чорне чорнило й чорне підкреслення. Нумерація в назві через «·»: `1 · Відносність руху`.

### 6.3 Сегментний перемикач `.seg`

Головний елемент керування серії — ним обирають систему відліку. Кнопки в один ряд, спільна рамка, радіус 8 px.

Активна кнопка забарвлюється **кольором тієї СВ, яку обрано**:

```css
.seg button.active{
  color: var(--ink);
  background: color-mix(in srgb, var(--c,#888) 14%, #fff);
  box-shadow: inset 0 -2px 0 var(--c,#888);
}
```

Колір передається на кнопку через `style="--c:var(--k2)"`. Так перемикач сам стає легендою: обрав помаранчеву — на сцені підпис спостерігача теж помаранчевий.

### 6.4 Кнопки

- `.ibtn` — квадратна 34×30 px для дій без слів: `⏸` / `▶` / `↺`.
- `.btn` — текстова, 6×11 px відступи, 12.5 px/600. Може мати другий рядок `<small>` з умовою: **«мін. знос»** / *cos α = −u/v*. Другий рядок — 400 вага, колір `--mut`.

### 6.5 Повзунок `.sl`

Підпис займає повний рядок над доріжкою, значення — праворуч, мінімальна ширина 74 px і вирівнювання по правому краю, щоб число не стрибало.

```html
<label class="sl">
  <span><var>V</var> — швидкість поїзда (K′) відносно землі</span>
  <input type="range" id="m1V" min="-15" max="15" step="0.5" value="8">
  <output id="m1Vo"></output>
</label>
```

Підпис завжди називає **що це і відносно чого**. «Швидкість поїзда» — неповний підпис; «швидкість поїзда відносно землі» — повний. У симуляціях про відносність руху це не педантизм, а суть.

Хелпер `slider(id, unit, dec, cb)` сам чіпляє `input`, форматує значення й додає одиницю; `id` виводу — це `id` повзунка з суфіксом `o`.

### 6.6 Рядок величини `.kv`

Назва ліворуч (`--sec`), значення праворуч (600, табличні цифри), між ними пунктирна лінія знизу. В назві можна показати формулу: `час переправи T = H / (v·sin α)`.

### 6.7 Позначка `.badge`

Овальна рамка, 10.5 px/600. Використовується для однієї-єдиної мети: **позначити інваріант**. `однакова в усіх СВ`, `однаковий в обох СВ`, `стала`. Це найважливіший смисловий елемент серії — саме інваріанти є суттю теми.

### 6.8 Чип `.chip`

Квадрат 10×10 px, радіус 3 px, кольору СВ. Ставиться перед назвою СВ у легенді, таблиці та підказці. Ніколи не є єдиним носієм сенсу — поруч завжди підпис.

### 6.9 Підказка `.tip`

Біле тло, рамка `--line`, радіус 8 px, тінь `0 2px 10px rgba(11,11,11,.08)` — єдина тінь у всій системі. Заголовок підказки (`t = 6,2 с`) кольору `--mut`, далі рядки «чип + назва СВ + значення».

---

## 7. Мова сцени на полотні

Сцена — це не ілюстрація, а креслення. У неї свій сталий словник.

### 7.1 Об'єкти

| Що | Як малюється |
|---|---|
| **Земля / дорога / насип** | суцільна смуга `CLR.ground`, зверху лінія `--axis` 1,5 px |
| **Вода** | смуга `CLR.water`, межі — лінії `--axis`; **межі прив'язані до координат y = 0 і y = H, а не до країв полотна** |
| **Рухоме тіло-носій СВ** (поїзд, авто, вагон) | заокруглений прямокутник: обведення 2 px кольором СВ, заливка тим самим кольором з α ≈ 0,10 |
| **Тіло, за яким стежимо** | коло `--ink` радіуса 5,5–6 px із білим кільцем 2 px |
| **Орієнтир, нерухомий відносно землі** | стовпчик `--mut` 1,5 px + сіре коло, або кущ `#e4e3db` з обведенням `#c9c7bd` |
| **Вісь СВ** | стрілка кольором СВ, точка початку відліку, підписи `O` / `O′` / `O″` і `x` / `x′` / `x″` |
| **Ціль** | флагшток 2 px `--ink` + трикутний прапорець |

**Вісь активної СВ товща:** 3 px проти 2 px у решти. Учень одразу бачить, чия це система.

### 7.2 Орієнтири — обов'язковий елемент

На кожній сцені мають бути об'єкти, нерухомі відносно кожної СВ. Без них перехід між СВ не читається: якщо на екрані рухається лише одне тіло, неможливо побачити, що змінилася саме система відліку. Стовпи вздовж дороги, кущі на березі, розмітка землі, стрілки течії — це не декор, а метрологія сцени.

### 7.3 Стрілки, точки, плашки

```js
arrow(g, x1,y1, x2,y2, color, w=2)  // наконечник 6,5+1,2w px, розхил 0,44 рад
dot(g, x,y, r, fill, ring)           // ring малюється 2 px поверх заливки
pill(g, text, x,y, opts)             // текст на плашці rgba(252,252,251,.92), радіус 9
```

Плашка `pill()` потрібна там, де підпис лягає поверх насиченої сцени (стрілки течії, дощ). Без неї текст губиться.

### 7.4 Штрихування

| Візерунок | Сенс |
|---|---|
| `[3,3]` | допоміжна геометрія: перехрестя графіка, паралелограм швидкостей, ідеальний шлях до цілі |
| `[2,3]` | пройдена траєкторія тіла |
| `[1,3]` | не використовується — надто дрібно, зливається в лінію |

Траєкторія малюється кольором `--sec`, а не кольором СВ: вона належить тілу, а не системі.

### 7.5 Підпис спостерігача

Лівий (або правий) верхній кут кожної сцени, два рядки:

```
СВ води                                    ← 12.5 px / 600, колір СВ
вода нерухома — човен пливе прямо за курсом ← 10.5 px, --mut
```

Перший рядок називає СВ, другий — **що з неї видно**. Другий рядок міняється разом із перемикачем і є основним пояснювальним текстом сцени.

### 7.6 Розміри тексту на полотні

| Призначення | px | Колір |
|---|---|---|
| Підпис СВ (перший рядок) | 12.5 bold | колір СВ |
| Пояснення (другий рядок) | 10.5 | `--mut` |
| Підпис вектора, ключове число | 11.5–12 bold | `--ink` |
| Підпис осі, змінної | 11–12 bold | колір СВ |
| Позначки шкали, одиниці | 9.5–10 | `--mut` |

---

## 8. Векторні діаграми

Кожна задача має власну панель «Швидкості», де вектори будуються в тому самому масштабі, що й міркування.

- **Складові — кольорами СВ, результат — `--ink`.** Учень бачить: чорне = те, що спостерігає обрана СВ.
- **Паралелограм добудовується штрихом `[3,3]` кольору `--axis`.**
- **Кут позначається дугою `--mut` 1,2 px** радіуса 20–30 px, підпис — на бісектрисі на радіусі 28–52 px.
- **Підписи векторів без стрілок над літерами.** `v`, `u`, `v + u` — а не `v⃗`: комбінаційний символ U+20D7 у більшості системних шрифтів ламає базову лінію. Векторність передає сама стрілка на діаграмі.
- **Масштаб діаграми обчислюється з фактичних значень**, а не з константи, і **однаковий по обох осях** — інакше намальований кут не дорівнює обчисленому, і діаграма бреше.

```js
// межі фігури в фізичних одиницях → масштаб і зсув, щоб нічого не обрізалося
const xs=[0, u, v*Math.cos(a), u+v*Math.cos(a)];
const xmin=Math.min(...xs), xmax=Math.max(...xs);
const kk=Math.min((w-70)/Math.max(1.2,xmax-xmin),(h-48)/(v*Math.sin(a)+0.4));
const ox=32-xmin*kk, oy=h-24;
```

---

## 9. Графіки

Використовується спільна функція `drawChart(box, opt)`.

**Поля:** ліворуч 44 px (місце під підписи шкали), праворуч 54 px (місце під прямий підпис серії), зверху 12, знизу 26.

**Правила:**
- Одна вісь значень. Дві величини різної природи — два графіки, ніколи не дві шкали на одному полі.
- Крок шкали добирається `niceStep()` з ряду 1 / 2 / 5 / 10.
- Сітка `--grid` 1 px; нульова лінія `--axis`.
- Лінія серії 2 px, `lineJoin: round`.
- **Прямий підпис серії в кінці лінії** (`K`, `K′`, `K″`) плюс легенда з чипами над графіком. Ідентичність ніколи не тримається на самому кольорі.
- Перехрестя при наведенні: штрихова вертикаль `--axis` `[3,3]`, на кожній серії — біла точка з кільцем кольору серії, підказка з усіма значеннями одночасно.
- Діапазон осі значень рахується з крайніх станів на всьому інтервалі, а не з поточного кадру — інакше графік «дихає» під час анімації.

---

## 10. Кадрування сцени — найважливіше технічне правило

Масштаб сцени рахується **з верхньої межі діапазону величин**, а не з їхніх поточних значень, і лишається сталим, поки студент крутить ручку. Зменшується він тільки тоді, коли вміст справді виліз за розрахункову межу.

Чому саме так. Якщо знаменник масштабу — та сама величина, яку тягнуть, то добуток `величина × sc` майже сталий: **малюнок займає ті самі пікселі при будь-якому значенні**. Тягнеш радіус — дуга виглядає однаково, змінюються лише підписи осей. Студент бачить «нічого не сталося» саме тоді, коли має побачити зміну. Найгірший випадок серії — резонанс: масштаб ділився на резонансну амплітуду, тож підйом амплітуди вдесятеро супроводжувався здрібненням кадру вдесятеро, і резонансу не було видно взагалі.

```js
/* Спільний хелпер. maxX/maxY — верхні межі діапазону; nowX/nowY — поточний
   розмах, потрібен лише як запобіжник. */
function fitScale(w,h,padW,padH,maxX,maxY,nowX,nowY){
  const X=Math.max(maxX,nowX||0), Y=Math.max(maxY,nowY||0);
  return Math.min((w-padW)/Math.max(1e-6,X),(h-padH)/Math.max(1e-6,Y));
}
// Кеплер: півширина максимальна при a = 5, піввисота a√(1−e²) — там само при e = 0
const AMAX=5;
const sc=fitScale(bb.w,bb.h,70,70,2*AMAX+0.6,2*AMAX+0.6,0,0);
```

Межі не вигадують: вони вже оголошені в коді — у `clamp(snap(x,крок),lo,hi)` всередині `grips()` і в таблицях смуг `GG:[[ключ,підпис,lo,hi,крок,…]]`. Правка масштабу — це винесення вже наявного числа, а не поява нової константи.

Коли діапазон такий широкий, що на нижньому його кінці сцена вироджується (орбіти 200 км і 36 000 км), беруть **проєктну** межу — ту, під яку сцену задумано, — і чесно кажуть підписом, що решта виходить за кадр. Якщо ж сцена дрібна вже за типових значень, це сигнал звузити діапазон ручки, а не повертати «дихання».

**Заїзд обмежують не часом, а кадром.** Там, де тіло за фіксовані T секунд може заїхати куди завгодно, трасу роблять сталою, а прогін обривають на її кінці. Тоді сильніша тяга видно тим, що тіло долає ту саму трасу швидше.

`frozen()` після цього лишається **підстраховкою**: він потрібен там, де від величини, яку тягнуть, залежить не масштаб, а положення.

```js
// Задача про річку: кадр центрує весь шлях човна у вибраній СВ
const pathEnd=(wat?k.vx-this.u:k.vx)*k.T;
const lo=Math.min(0,pathEnd), hi=Math.max(0,pathEnd);
const sc=Math.min((h-96)/H,(w-48)/Math.max(34,hi-lo));
const ox=(w-(hi-lo)*sc)/2-lo*sc;
```

Три наслідки, які треба тримати в голові:

1. **Масштаб по x і по y — той самий.** Інакше кути й траєкторії спотворюються, а вся суть задачі в кутах.
2. **Усе, що має фізичну координату, малюється через `SX()`/`SY()`.** Береги річки, ґрунт, шкала — теж. Заливка «від краю до краю полотна» ламається, щойно масштаб зміниться.
3. **Крок підписів шкали залежить від масштабу**, щоб цифри не зливалися: `sc>4.2 ? 10 : sc>2.2 ? 20 : 40`. Позначки за межами полотна пропускаються.

Коли об'єкт таки не влазить (наприклад, ціль відпливла разом із берегом), на краю малюється **вказівник зі стрілкою й підписом**, а не мовчазне зникнення.

---

## 11. Анімація та керування

- Один цикл `requestAnimationFrame` на всю сторінку; тікає **тільки активний модуль**.
- Крок часу обмежується згори: `dt = Math.min(0.05, (now-last)/1000)`. Після повернення на вкладку симуляція не стрибає.
- Швидкість сцени задається множником усередині модуля (наприклад, ×2.5 для річки, ×1.4 для поїздів), а не зміною фізичних величин.
- **Зміна будь-якого повзунка скидає сценарій** на початок. Півдороги змінені умови — джерело хибних висновків.
- Сценарій із фіналом (переправа, зустріч, приземлення) після завершення тримає кадр 1,4–2,2 с і починає спочатку. Кнопки «повтор» достатньо, автоцикл не дратує.
- Керування пуском — `⏸`/`▶`; після завершення та сама кнопка перезапускає.
- Перемикання СВ **не скидає час**: учень має бачити ту саму мить із іншої системи. Це головний жест усієї серії.

---

## 12. Технічний каркас

**Один файл.** HTML + CSS + JS в одному документі, без збірки, без залежностей, без мережевих запитів. Матеріал має відкриватися з флешки в кабінеті без інтернету.

**Полотна через хелпер із урахуванням DPR:**

```js
function cvx(id){
  if(CV[id]) return CV[id];
  const c=$(id), r=c.getBoundingClientRect();
  if(r.width<2||r.height<2) return null;          // прихована вкладка
  const d=window.devicePixelRatio||1;
  c.width=Math.round(r.width*d); c.height=Math.round(r.height*d);
  const g=c.getContext('2d'); g.setTransform(d,0,0,d,0,0);
  return CV[id]={g,w:r.width,h:r.height,el:c};
}
window.addEventListener('resize',()=>{for(const k in CV) delete CV[k];});
```

Повернення `null` для прихованого полотна обов'язкове: модулі на неактивних вкладках мають нульовий розмір.

**Контракт модуля.** Кожен модуль — об'єкт із трьома методами:

```js
const MX = {
  init(){ /* підписка на керування, початковий стан */ },
  tick(dt){ /* просунути стан і викликати render */ },
  render(){ /* намалювати все з поточного стану */ }
};
```

`render()` не має права змінювати стан — тільки читати. Завдяки цьому будь-який кадр відтворюється підстановкою стану, що робить симуляцію придатною до автоматичної перевірки.

**Навігація за хешем:** `#m2`, `#m3c` відкривають потрібний модуль і підрозділ. Зручно для посилань у конспекті.

---

## 13. Мова

Українська, науковий стиль без канцеляриту.

- **СВ** — усталене скорочення, вживається вільно. **ІСВ** — лише там, де інерціальність саме зараз важлива.
- Позначення систем: `K`, `K′`, `K″` (штрих — U+2032, подвійний штрих — U+2033, не апострофи).
- Грецькі літери — символами: `α`, `φ`, `θ`, `ρ`, не транслітом.
- Підписи задач у навігації — двома рядками: назва + що саме з'ясовуємо. «Переправа через річку» / «вибір курсу проти течії».
- Наказовий спосіб у підказках до дії: «Перетягуйте точку», «Порівнюйте картину руху». Не «можна перетягнути».

---

## 14. Доступність

- Ідентичність ніколи не тримається лише на кольорі: колір + чип + прямий підпис.
- Зелений (`--k3`) і будь-який колір з контрастом нижче 3:1 обов'язково супроводжується підписом.
- Мінімальний розмір тексту на полотні — 9,5 px і лише для шкали; усе змістовне — від 10.5 px.
- Ціль натискання не менша за 30×30 px (`.ibtn` — 34×30).
- Полотна, які можна тягнути, мають `touch-action: none` і `cursor: grab`.
- Перевірку палітри проганяти скриптом, а не оком:
  ```
  node dataviz/scripts/validate_palette.js "#2a78d6,#eb6834,#1baf7a" --mode light --pairs all
  ```

---

## 15. Антипатерни

Помилки, що вже траплялися в цій серії, — перевіряй саме їх.

| Антипатерн | Чому погано | Як правильно |
|---|---|---|
| Заливка сцени від краю до краю полотна | При зміні масштабу «берег» перестає збігатися з координатою y = H | Малювати через `SY()` |
| Фіксований масштаб сцени | При крайніх значеннях повзунків тіло виїжджає за кадр | Обчислювати масштаб із фактичного розмаху |
| Різний масштаб по x і y | Намальований кут не дорівнює обчисленому | Один `sc` на обидві осі |
| Два подання поруч у різних масштабах | Висоти й довжини не можна порівняти візуально | Спільний `sc`, обчислений один раз |
| Поле кадру пропорційне кожній осі окремо | На плескатій траєкторії вертикальне поле з'їдає масштаб, і фігура стає вдвічі меншою за потрібну | Одне поле від більшого з розмахів: `m = max(rx,ry)*0.05 + 0.4` |
| Прямі підписи серій у кінці ліній без розведення | При близьких значеннях (наприклад `ω` і `v` при `R` = 1 м) підписи лягають один на одного | Відсортувати по `y` і розсунути на 13 px |
| Масштаб векторів від максимуму по всьому тілу | Стрілки в точці біля осі лишаються мікроскопічними — розклад не читається | Масштабувати від максимуму саме тієї точки, яку показуємо |
| Камера стежить за тілом, а тіло малюється в старих координатах | Тіло зникає за краєм | Усі об'єкти через одне перетворення `X()` |
| Символи `v⃗`, `u⃗` у підписах | Комбінаційна стрілка ламає базову лінію | `v`, `u` + стрілка на діаграмі |
| Число без табличних цифр в анімації | Цифри стрибають по горизонталі | `tabular-nums` |
| Дефіс замість мінуса | Читається як переніс | U+2212 через `fmt()` |
| Підпис «швидкість човна» | Невідомо, відносно чого | «швидкість човна відносно води» |
| Легенда без прямих підписів на графіку | Погляд бігає між легендою і лінією | Обидва: легенда + підпис у кінці лінії |
| Автоматичне скидання при зміні повзунка забуте | Учень бачить суміш двох сценаріїв | `reset()` у кожному обробнику |
| Масштаб продубльовано в обробнику вказівника | Влучання рахують за одними числами, а малюють за іншими — ручка «промахується» повз об'єкт | Одна `geo(b)` і для `scene()`, і для `grips()` |
| `cursor:grab` або підпис «перетягуйте» без обробника | Інтерфейс обіцяє жест, якого немає | Або `grip()`, або прибрати обіцянку |
| Ручка без підказки при наведенні | Учень не знає, що саме зміниться | `hint` у кожній ручці |
| Кадр не заморожено на час жесту | Тягнеш величину → росте розмах → падає `sc` → ручка тікає з-під пальця | `frozen(this,b,…)` у `geo()` |
| Ручка, положення якої по одній осі залежить від тієї ж величини | Під час горизонтального жесту ручка сповзає по вертикалі | Одна вісь — одна величина; друга координата стала |

---

## 16. Чекліст перед публікацією

**Фізика**
- [ ] Числа на екрані звірені з незалежним розрахунком (окремий скрипт перевірок, а не «на око»)
- [ ] Інваріанти позначені `.badge` і справді не змінюються при перемиканні СВ
- [ ] Крайні значення повзунків не дають NaN, ділення на нуль чи виродженої геометрії

**Візуальне**
- [ ] Кожна вкладка й підрозділ відкриті та переглянуті скриншотом
- [ ] Жоден підпис не перекриває інший при крайніх значеннях повзунків
- [ ] Кольори СВ однакові в усіх модулях
- [ ] Немає горизонтальної прокрутки на 760 px
- [ ] Порожніх зон понад третину висоти полотна немає

**Технічне**
- [ ] `node --check` на витягнутому скрипті проходить
- [ ] Жодного зовнішнього ресурсу: `grep -nE "https?://|src=|@import|fetch\(" файл.html` порожній
- [ ] Консоль без помилок на всіх станах
- [ ] Робота після `resize` вікна

---

## 17. Іменування

- Тека розділу — англійською з великої літери за розділом курсу: `Mechanics/`, `Optics/`. Якщо замовник назвав теку інакше (`basic_physics/`), використовується його назва — вона фіксується у списку структури репозиторію на початку документа.
- Файл симуляції — англійський kebab-case за темою: `inertial-frames.html`, `oscillations.html`.
- `id` елементів — префікс модуля + коротка назва: `m1V`, `m3aRe`, `m2cart`. Вивід повзунка — `id` + `o`.
- Об'єкти модулів — `M1`, `M2`, `M3`; підрозділи задач — `M3A`…`M3D`.
- Коментарі в коді — українською, тільки там, де пояснюють **фізичне чи геометричне обмеження**, а не роботу коду.

---

## 18. Тематичні мапінги кольорів

§2.1 закріплює `--k1` / `--k2` / `--k3` за системами відліку. Це правило чинне там, **де системи відліку є**. У темах, де їх немає (кінематика однієї точки, координати, хвилі, поля), три слоти дістають інші ролі — але за трьома жорсткими умовами:

1. **Ролі призначаються один раз на файл** і не міняються між вкладками. Учень має запам'ятати відповідність із першої вкладки й користуватися нею до кінця.
2. **Мапінг виписаний коментарем у шапці `<script>`** і продубльований тут таблицею. Без цього наступна людина призначить кольори по-своєму.
3. **Ролі однотипні за рівнем**: не можна змішати «швидкість», «поїзд» і «зелений колір фону» — усі три мають бути величинами або об'єктами того самого ґатунку.

Решта правил §2 лишається без змін: `--ink` — завжди результат, `--sec` — завжди пройдена траєкторія, зелений `--k3` ніколи не несе сенсу без підпису.

**Приклад 1 — [Mechanics/inertial-frames.html](Mechanics/inertial-frames.html), базовий мапінг:**

| Токен | Роль |
|---|---|
| `--k1` | СВ K — нерухома: земля, берег, дорога |
| `--k2` | СВ K′ — перша рухома: поїзд, авто, вагон |
| `--k3` | СВ K″ — друга рухома: велосипедист, вода, поїзд 2 |

**Приклад 2 — [basic_physics/00-kinematics-rotation.html](basic_physics/00-kinematics-rotation.html), тема без систем відліку:**

| Токен | Роль | Де застосовано |
|---|---|---|
| `--k1` | **положення** | радіус-вектор, координати, переміщення; широта і паралель на глобусі; СВ дороги в модулі кочення |
| `--k2` | **рух і обертання** | `v`, `ω`, `φ`, `ε`, дуги кутів; довгота і меридіан; обід колеса, СВ колеса; напрям на Сонце |
| `--k3` | **прискорення та нерухома зовнішня опора** | `a`<sub>n</sub>, `a`<sub>τ</sub>; небесна сфера, точка весняного рівнодення, напрям на далекі зорі |

Зверни увагу: у модулі про кочення колеса, де системи відліку таки з'являються, `--k1` і `--k2` природно збігаються з базовим мапінгом (нерухома дорога — синій, рухоме колесо — помаранчевий). Так і має бути: тематичний мапінг вибирають сумісним із базовим, а не всупереч йому.

**Дрібний текст** і далі бере затемнені варіанти з §2.4 (`.c1` / `.c2` / `.c3`) — правило «колір для геометрії, затемнений варіант для тексту» від зміни ролей не залежить.

**Астрономічні знаки** пиши друкованою формою, а не емодзі: `ϒ` (точка весняного рівнодення), а не `♈` — емодзі-презентація дає кольорову плашку, що вибивається з палітри.

---

## 19. Пряме керування на сцені

Величину задають на кресленні, а не повзунком у бічній колонці. Бічна колонка
показує **числа**; сцена — місце, де їх **змінюють**.

### Що чим стає

| Що задаємо | Чим тягнемо |
|---|---|
| Положення, відстань, висота | сам об'єкт; окремий параметр «відстань» зникає |
| Довжина, площа, переріз, зазор | край тіла — це і є фізика: `ρl/S`, `εε₀S/d` |
| Кут | точка на дузі кута або кінець самого тіла |
| Модуль вектора | наконечник стрілки; напрям і модуль — одним жестом |
| Скаляр, прив'язаний до тіла | розмір тіла або кільце на його ободі, радіус ∝ величині |
| Властивість речовини (`μ`, `ε`, `ρ`, `γ`) | **полиця зразків** `shelf()` унизу сцени: клацання по підписаному зразку |
| Температура | **термометр** `therm()` — фізичний прилад, а не смужка |
| Номінал (запобіжник, автомат) | полиця стандартних номіналів |
| Величина без геометричного образу | **смуга керування** `gauge()` під кресленням, у власній висоті полотна |

### Обов'язки модуля з ручками

1. **`geo(b)`** — чиста, повертає **пряме й зворотне** перетворення (`SX/SY`,
   `iX/iY`) і всі похідні розміри. `scene()` більше не рахує масштаб сам.
2. **`frozen(this,b,…)`** — обгортка навколо `geo()` скрізь, де масштаб чи
   положення тіла залежать від величини, яку тягнуть. Без неї ручка тікає
   з-під пальця вже на першому кадрі жесту.
3. **`grips()`** — чиста, повертає ручки в **екранних пікселях**; `move()`
   захоплює `geo()` у замиканні й сам робить зворотне перетворення з
   `clamp()`/`snap()`.
4. **`init()`** — один `grip('mNscene',this,()=>this.grips())` замість усіх
   `slider()`.
5. **`scene()`** — передостаннім рядком `for(const hh of this.grips()) drawGrip(g,hh,this);`.
6. Розгортка часу не втручається, поки ручку тримають: `if(this._grab==null)`.

### Словник ручки (`drawGrip`)

| Стан | Як малюється |
|---|---|
| спокій | коло радіуса `h.r` кольором об'єкта, біле кільце 2 px |
| наведення | + зовнішнє кільце `--sec` 1,2 px і підпис `h.hint` 11 px bold |
| захоплення | кільце `--ink` 2 px, підпис лишається |

`bare:true` — ручкою є сам об'єкт сцени (заряд, тіло, пробна точка): свого
кружка вона не малює, інакше накрила б те, за що тягнуть.

### Доступність

`grip()` робить полотно фокусованим (`tabIndex`), `Tab` перебирає ручки,
стрілки зсувають на 4 px, `Shift` — на 16. Без цього переїзд керування з
повзунків на полотно був би системною регресією доступності (§14).

### Коли повзунок іще припустимий

Величина, яка має в кресленні власний розмір — довжина, кут, висота стовпця, —
правиться **тільки** ручкою на полотні: її тягнуть саме за те, що вона означає.

Виняток — величини **без геометричного образу**. Коли таких у модулі кілька,
вони йдуть повзунками `input type=range` у бічній колонці, а не смугами
`gauge()` під кресленням: чотири-п'ять смуг з'їдають нижню третину кадру, і
схема від цього не стає зрозумілішою. Смуга `gauge()` лишається доречною там,
де величина одна-дві й вони читаються поряд із кресленням.

Правила для такої колонки:

- **Один шлях на величину.** Якщо величину править повзунок, ручки на полотні
  для неї немає, і навпаки — два способи на одне число читаються як
  дублювання (пор. §15).
- **Один рядок на величину.** Підпис повзунка — це і є підпис величини;
  окремого рядка `.kv` з тим самим числом у панелі бути не повинно.
- **Повзунок звіряється зі станом щокадру.** Стан міняє не лише палець:
  перемикач режиму, `reset()`, затискачі на кшталт `clamp()`. `bindSlider()`
  реєструє повзунок, `syncSliders(st)` у `gdraw()` щокадру підтягує положення
  й число до моделі — з порогом у півкроку, щоб не смикалося на округленні.

У серії повзунки в бічній колонці мають `00`, `01`, `05`, `07`, `10`, `11`, `13`;
решта файлів керується виключно ручками на полотні.
