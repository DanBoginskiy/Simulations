# Шаблон промпту для нової симуляції серії

Заповни блок «Замовлення» — усе інше не чіпай. Незмінна частина повторює вимоги
[BRANDBOOK.md](BRANDBOOK.md) у формі, придатній для передавання виконавцеві.

Мінімум, який треба заповнити: `{{ТЕМА}}`, `{{ПРОСТИЙ ПРИКЛАД}}`, `{{СКЛАДНИЙ ПРИКЛАД}}`.
Решту можна лишити як є — значення за замовчуванням указані в дужках.

---

## ▼ ЗАМОВЛЕННЯ — редагуй тільки цю частину

```
ТЕМА:              {{ТЕМА}}
ПАПКА:             {{ПАПКА}}                  (за замовчуванням: розділ курсу, напр. Mechanics/)
ІМ'Я ФАЙЛУ:        {{ФАЙЛ}}.html              (kebab-case англійською за темою)
КІЛЬКІСТЬ ВКЛАДОК: {{N}}                      (за замовчуванням 4–5, одна вкладка — одна ідея)

ПРОСТИЙ ПРИКЛАД (перша вкладка, «словник теми»):
  {{ПРОСТИЙ ПРИКЛАД}}
  режими / перемикачі: {{РЕЖИМИ}}
  що тягнемо на сцені: {{ЩО ТЯГНЕМО НА СЦЕНІ}}

СКЛАДНИЙ ПРИКЛАД (життєва ситуація, де тема працює по-справжньому):
  {{СКЛАДНИЙ ПРИКЛАД}}
  що саме має стати очевидним: {{ВИСНОВОК}}

ДОДАТКОВІ МОДУЛІ:
  {{ДОДАТКОВІ МОДУЛІ}}

ГРАФІКИ (величина(и) на кожному; різні одиниці — різні графіки):
  {{ГРАФІКИ}}

ІНВАРІАНТИ для позначки .badge (величини, що не змінюються — суть теми):
  {{ІНВАРІАНТИ}}

РОЛІ КОЛЬОРІВ (якщо в темі немає систем відліку — перепризначити явно):
  --k1: {{РОЛЬ K1}}
  --k2: {{РОЛЬ K2}}
  --k3: {{РОЛЬ K3}}

ЧИСЛА, ЯКІ ТРЕБА ЗВІРИТИ ОКРЕМИМ СКРИПТОМ:
  {{КОНТРОЛЬНІ ЧИСЛА}}
```

---

## ▼ НЕЗМІННА ЧАСТИНА — не редагуй

### Що зробити

Створи одну самодостатню симуляцію за темою й прикладами вище, суворо за
[BRANDBOOK.md](BRANDBOOK.md). Еталонна реалізація, з якої беруться CSS-каркас і
хелпери, — [Mechanics/inertial-frames.html](Mechanics/inertial-frames.html);
другий приклад стилю — [basic_physics/00-kinematics-rotation.html](basic_physics/00-kinematics-rotation.html).

### Технічні вимоги

- **Один файл.** HTML + CSS + JS в одному документі. Без збірки, без залежностей,
  без мережевих запитів, без зовнішніх шрифтів і картинок: матеріал має відкриватися
  з флешки без інтернету.
- **Повторно використай хелпери еталона дослівно**, не переписуй їх:
  `$`, `CLR`, `fmt`, `DEG`, `cvx` (+ скидання кеша полотен на `resize`), `seg`,
  `slider`, `txt`, `arrow`, `pill`, `dot`, `niceStep`, `drawChart`, `makeTip`, `hover`.
- **Контракт модуля** — об'єкт із трьома методами:
  `init()` — підписка на керування й початковий стан;
  `tick(dt)` — просунути стан і викликати `render()`;
  `render()` — намалювати все з поточного стану. **`render()` не змінює стан.**
- Один цикл `requestAnimationFrame` на сторінку; тікає лише активний модуль;
  `dt = Math.min(0.05, (now - last) / 1000)`.
- Навігація за хешем: `#m2`, `#m3` відкривають потрібну вкладку.
- `cvx()` повертає `null` для полотна нульового розміру (прихована вкладка) — це обов'язково.

### Змістові вимоги

- **Показувати, а не розповідати.** Текст — підпис до того, що вже видно.
- **Кожна анімована величина має живий числовий вивід.**
- **Формула з підставленими числами:** символьний запис, далі ті самі числа,
  у тому ж рядку: `v = √(10,3² + (−10,7)²) = 14,8 м/с`.
- **Інваріанти позначені `.badge`** — це найважливіший смисловий елемент серії.
- **Підпис величини називає, що це і відносно чого.** «Швидкість човна» — неповний
  підпис; «швидкість човна відносно води» — повний.
- **Кожну величину задають на сцені** (§19): жодного `input type=range` без
  письмового обґрунтування. Бічна колонка показує числа, сцена — змінює їх.
- **Підпис сцени у два рядки** (лівий верхній кут): назва стану / що саме з нього видно.
- **Зміна будь-якої величини скидає сценарій** на початок; поки ручку тримають,
  розгортка часу не втручається.
- **Перемикання системи відліку не скидає час.**

### Оформлення

- Українська, науковий стиль. Змінні — у `<var>`; грецькі літери символами (`α`, `φ`, `ω`).
- Числа лише через `fmt()`: кома як роздільник, мінус U+2212, фіксована розрядність,
  `tabular-nums` скрізь, де числа стоять стовпчиком або швидко змінюються.
- Палітра, типографіка, сітки, компоненти — рівно ті, що в брендбуці. Нічого не додавати.
- Коментарі в коді — українською й лише там, де пояснюють **фізичне чи геометричне
  обмеження**, а не роботу коду.

### Кадрування (найчастіше джерело помилок)

- Масштаб сцени рахується **з верхньої межі діапазону** (`fitScale()`), а не з поточних значень, і стоїть нерухомо, поки крутять ручку. Інакше `величина × sc` майже стале й зміна величини стає невидимою.
- **Один масштаб на обидві осі.** Інакше намальований кут не дорівнює обчисленому.
- Усе, що має фізичну координату, малюється через `SX()`/`SY()` — заливки й межі теж.
- Два подання поруч — спільний масштаб, обчислений один раз.
- Об'єкт, що не влазить, показується вказівником зі стрілкою на краю, а не зникає.
- Поля кадру однакові по обох осях, інакше плеската траєкторія без потреби стискає масштаб.

### Діапазони графіків

Рахуються з крайніх станів **усього** інтервалу, а не з поточного кадру — інакше
графік «дихає» під час анімації.

---

## ▼ ЧЕКЛІСТ ПРИЙМАННЯ

**Фізика**
- [ ] Написано окремий скрипт перевірок; усі контрольні числа з блоку «Замовлення» звірені ним, а не «на око»
- [ ] Інваріанти позначені `.badge` і справді не змінюються
- [ ] Крайні значення кожної величини не дають `NaN`, `Infinity`, ділення на нуль чи виродженої геометрії
- [ ] Жодного `input type=range` без письмового обґрунтування (§19)
- [ ] Кожну ручку протягнуто в крайні положення кадру; ручка не тікає з-під пальця

**Візуальне**
- [ ] Кожна вкладка й підрозділ відкриті та переглянуті скриншотом
- [ ] Жоден підпис не перекриває інший при крайніх значеннях величин
- [ ] Ручки не накривають те, за що тягнуть, і мають підказку при наведенні
- [ ] Ролі кольорів однакові в усіх модулях і виписані коментарем у шапці скрипта
- [ ] Немає горизонтальної прокрутки на 760 px
- [ ] Порожніх зон понад третину висоти полотна немає

**Технічне**
- [ ] `node --check` на витягнутому скрипті проходить
- [ ] `grep -nE "https?://|src=|@import|fetch\(" файл.html` — порожньо
- [ ] Консоль без помилок на всіх станах
- [ ] Робота після `resize` вікна

---

## ▼ ПРИКЛАД ЗАПОВНЕННЯ

Так виглядало замовлення на [basic_physics/00-kinematics-rotation.html](basic_physics/00-kinematics-rotation.html):

```
ТЕМА:              Кінематика поступального і обертального руху: основні поняття
                   і величини кінематики. Зоряний час. Географічні координати.
ПАПКА:             basic_physics/
ІМ'Я ФАЙЛУ:        kinematics-rotation.html
КІЛЬКІСТЬ ВКЛАДОК: 5

ПРОСТИЙ ПРИКЛАД:
  рух матеріальної точки — радіус-вектор, шлях, переміщення, v, a_τ, a_n, енергія
  режими: рівномірний прямолінійний · рівноприскорений (кидок під кутом) · рівномірний по колу
  повзунки: v і β · v₀ і α · R і ω · маса m (спільна)

СКЛАДНИЙ ПРИКЛАД:
  кочення колеса без ковзання дорогою
  що має стати очевидним: реальний рух = поступальний + обертальний;
  точка дотику нерухома, верхня точка йде вдвічі швидше за авто

ДОДАТКОВІ МОДУЛІ:
  обертання твердого тіла — відповідність s↔φ, v↔ω, a↔ε; ω спільна, v — ні
  зоряний час — Земля на орбіті: зоряна доба проти сонячної, два годинники
  глобус і небесна сфера — φ, λ, α, δ, зоряний час S, годинний кут t

ГРАФІКИ:
  координати x(t), y(t) · швидкість |v|, v_x, v_y · енергія Eк, Eп, E
  кут повороту φ(t) · кутова ω(t) разом із лінійними v_A(t), v_B(t)
  |v_P|(t) і y_P(t) для точки обода
  випередження зоряного годинника (хв) · момент кульмінації зорі (год)
  v = ωR·cos φ від широти · висота світила над горизонтом за добу

ІНВАРІАНТИ:
  повна енергія при кидку — стала
  ω і ε — однакові для всіх точок тіла
  швидкість точки дотику колеса — нуль (миттєвий центр швидкостей)
  зоряних діб у році рівно на одну більше, ніж сонячних
  ω Землі — однакова для всіх точок; 1 год зоряного часу = 15° довготи

РОЛІ КОЛЬОРІВ:
  --k1: положення — радіус-вектор, координати, переміщення; широта і паралель; СВ дороги
  --k2: рух і обертання — v, ω, φ, ε, дуги кутів; довгота і меридіан; СВ колеса; напрям на Сонце
  --k3: прискорення (a_n, a_τ) і нерухома зовнішня опора — небесна сфера, точка ϒ, далекі зорі

КОНТРОЛЬНІ ЧИСЛА:
  ω Землі = 7,292·10⁻⁵ рад/с; v на екваторі = 464,6 м/с; a_n = 0,0339 м/с²
  зоряна доба 23 г 56 хв 04,1 с; різниця з сонячною 3 хв 55,9 с
  366,2422 / 365,2422 = 1,0027379; кут по орбіті 0,986°/добу
  шлях точки обода за оберт = 8R; |v_P| = 2v_c·|sin(θ/2)|
  E = const при кидку з похибкою < 1e-9
```
